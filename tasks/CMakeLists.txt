# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(simple_path_plannerTaskLib)
ADD_LIBRARY(${SIMPLE_PATH_PLANNER_TASKLIB_NAME} SHARED 
    ${SIMPLE_PATH_PLANNER_TASKLIB_SOURCES})

add_dependencies(${SIMPLE_PATH_PLANNER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${SIMPLE_PATH_PLANNER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${SIMPLE_PATH_PLANNER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${SIMPLE_PATH_PLANNER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${SIMPLE_PATH_PLANNER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${SIMPLE_PATH_PLANNER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${SIMPLE_PATH_PLANNER_TASKLIB_HEADERS}
    DESTINATION include/orocos/simple_path_planner)

